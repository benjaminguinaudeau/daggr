% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/edges.R
\name{dag_edge}
\alias{dag_edge}
\title{Define a single edge specification}
\usage{
dag_edge(from, to, ...)
}
\arguments{
\item{from}{A ggdiagram shape object (the arrow's origin).}

\item{to}{A ggdiagram shape object (the arrow's destination).}

\item{...}{Per-edge arguments passed to \code{\link[ggdiagram:connect]{ggdiagram::connect()}}
(e.g., \code{linetype}, \code{arc_bend}). These override the shared defaults
set in \code{\link[=dag_edges]{dag_edges()}}.}
}
\value{
A \code{dag_edge_spec} object (a named list).
}
\description{
Creates a lightweight edge specification for use inside \code{\link[=dag_edges]{dag_edges()}}.
Each spec captures the \code{from} and \code{to} nodes plus any per-edge overrides
(e.g., \code{linetype}, \code{arc_bend}).
}
\examples{
library(daggr)
A <- dag_ellipse("X")
B <- dag_ellipse("Y") |> place(from = A, where = "right", sep = 2.5)
dag_edge(A, B, linetype = "dashed")
}
