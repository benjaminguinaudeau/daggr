% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/edges.R
\name{dag_edges}
\alias{dag_edges}
\title{Create multiple DAG edges at once}
\usage{
dag_edges(..., resect = 2, color = "black", alpha = 1)
}
\arguments{
\item{...}{One or more \code{\link[=dag_edge]{dag_edge()}} specifications.}

\item{resect}{Amount to pull back arrows from node boundaries.
Defaults to \code{2}.}

\item{color}{Arrow color. Defaults to \code{"black"}.}

\item{alpha}{Arrow opacity. Defaults to \code{1}.}
}
\value{
A list of connection objects, suitable for adding to a
\code{ggdiagram()} plot with \code{+}.
}
\description{
Takes multiple \code{\link[=dag_edge]{dag_edge()}} specifications and builds all connections in
one call. Shared styling (e.g., \code{resect}, \code{color}) is specified once;
per-edge overrides in each \code{\link[=dag_edge]{dag_edge()}} take precedence.
}
\details{
Returns a plain \code{list()} of connection objects, which \code{ggplot2}'s \code{+}
operator handles natively (iterating over each element).
}
\examples{
library(daggr)
X <- dag_ellipse("X")
M <- dag_ellipse("M") |> place(from = X, where = "right", sep = 2.5)
Y <- dag_ellipse("Y") |> place(from = M, where = "right", sep = 2.5)

ggdiagram() +
  X + M + Y +
  dag_edges(
    dag_edge(X, M),
    dag_edge(M, Y),
    dag_edge(X, Y, arc_bend = 0.4)
  ) +
  theme_dag()
}
