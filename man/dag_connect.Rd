% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/edges.R
\name{dag_connect}
\alias{dag_connect}
\title{Connect two DAG nodes with an arrow}
\usage{
dag_connect(from, to, resect = 2, color = "black", alpha = 1, ...)
}
\arguments{
\item{from}{A ggdiagram shape object (the arrow's origin).}

\item{to}{A ggdiagram shape object (the arrow's destination).}

\item{resect}{Amount to pull back the arrow from the node boundary.
Defaults to \code{2}.}

\item{color}{Arrow color. Defaults to \code{"black"}.}

\item{alpha}{Arrow opacity. Defaults to \code{1}.}

\item{...}{Additional arguments passed to \code{\link[ggdiagram:connect]{ggdiagram::connect()}}.}
}
\value{
A connection object suitable for adding to a \code{ggdiagram()} plot.
}
\description{
Thin wrapper around \code{\link[ggdiagram:connect]{ggdiagram::connect()}} with DAG-friendly defaults:
\code{resect = 2}, black color, full opacity.
}
\examples{
library(daggr)
A <- dag_ellipse("X")
B <- dag_ellipse("Y") |> place(from = A, where = "right", sep = 2.5)
dag_connect(A, B)
}
